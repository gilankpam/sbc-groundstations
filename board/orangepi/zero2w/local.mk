# Orange Pi Zero 2W package overrides (BR2_PACKAGE_OVERRIDE_FILE).
#
# ffmpeg is now the standalone package/ffmpeg-v4l2request (jernejsk 7.1 +
# V4L2-Request hwaccel, downloaded by Buildroot), so there is no longer an
# FFMPEG_OVERRIDE_SRCDIR here. Add per-package OVERRIDE_SRCDIR lines below for
# local development as needed.
